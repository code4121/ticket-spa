import { expect } from "chai";
import "mocha";
import { Ticket } from "../../entities/ticket";
import { LocalTicketRepository } from "../../services";

describe.skip("Creates", () => {
  const localTicketRepository = new LocalTicketRepository();
});
